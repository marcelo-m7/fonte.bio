name: Frontend CI

on:
  push:
    paths:
      - "fonte.tube/frontend/**"
      - ".github/workflows/frontend-ci.yml"
  pull_request:
    paths:
      - "fonte.tube/frontend/**"
      - ".github/workflows/frontend-ci.yml"

jobs:
  frontend:
    name: Lint Typecheck Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: fonte.tube/frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: fonte.tube/frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

      - name: Build
        run: pnpm build
